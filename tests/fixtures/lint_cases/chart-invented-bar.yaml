# NO DATA must never become a bar.
rule: chart-invented-bar
level: error
patches:
  - {file: pages/03-short.yaml, op: append, path: blocks, value: {range_chart: {axis: {min: 0, max: 10, step: 2}, rows: [{label: X, claim: n-1, lo: 1, hi: 3}]}}}
